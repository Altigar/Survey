{% extends 'base.html.twig' %}

{% block title %} {{ title }} {% endblock %}

{% block body %}
    {% include('partials/navigation.html.twig') %}
    <list-layout
        passes="{{ passes|serialize('json', ['answers', 'survey', 'externalPerson', 'person']) }}"
        survey-id="{{ survey.id }}"
    ></list-layout>
{% endblock %}