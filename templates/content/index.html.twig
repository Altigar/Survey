{% extends 'base.html.twig' %}

{% block title %} {{ title }} {% endblock %}

{% block body %}
    {% include('partials/navigation.html.twig') %}
    <content-layout id="{{ survey.id }}" questions="{{ questions|serialize('json', ['answers', 'survey']) }}"></content-layout>
{% endblock %}