<ul class="row list-unstyled mb-2 gx-2">
    <li class="col-md mb-2 text-center">
        <a class="bg-white border rounded flicker nav-link text-dark" href="{{ path('content', {survey: survey.id}) }}">Content</a>
    </li>
    <li class="col-md mb-2 text-center">
        <a class="bg-white border rounded flicker nav-link text-dark" href="{{ path('survey_edit', {survey: survey.id}) }}">Edit</a>
    </li>
    <li class="col-md mb-2 text-center">
        <a class="bg-white border rounded flicker nav-link text-dark" href="{{ path('stats', {survey: survey.id}) }}">Stats</a>
    </li>
    <li class="col-md mb-2 text-center">
        <a class="bg-white border rounded flicker nav-link text-dark" href="{{ path('stats_people', {survey: survey.id}) }}">People</a>
    </li>
    <li class="col-md mb-2 text-center">
        <a class="bg-white border rounded flicker nav-link text-dark" href="{{ path('share', {survey: survey.id}) }}">Share</a>
    </li>
</ul>